async function refreshAll(render=true){ await loadAll(); renderProductSelectors(); renderRecipeSelectors(); if(render){ renderDashboard(); renderProducts(); renderRecipes(); renderIngredients(); renderSales(); renderExpenses(); renderAnalytics(); renderCartSimulator(); renderSettings(); renderUsers(); } }
document.addEventListener('DOMContentLoaded', async()=>{ const sidebar=byId('sidebar'); if(localStorage.getItem('sidebarCollapsed')==='true'){ sidebar.classList.add('collapsed'); document.body.classList.add('sidebar-collapsed'); } if(!supabaseClient) showError('Supabase client could not load. Check internet and Supabase config.'); await refreshAll(false); showTab('dashboard'); });

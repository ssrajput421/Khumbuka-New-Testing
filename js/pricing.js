function calculatePricing(){ const cost=num(val('pricingProductCost'))+num(val('pricingPackagingCost')); const commission=num(val('pricingCommission'))/100; const gst=num(val('pricingGST'))/100; const desired=num(val('pricingDesiredMargin'))/100; const offline= cost / Math.max(0.01,1-desired); const online= cost / Math.max(0.01,1-commission-gst-desired); const offlineProfit=offline-cost; const onlineProfit=online-(online*commission)-(online*gst)-cost; byId('pricingOutput').innerHTML=`<div class="result-card"><h3>Suggested Pricing</h3><div class="stat-row"><span>Suggested Offline Price</span><strong>${money(offline)}</strong></div><div class="stat-row"><span>Suggested Online Price</span><strong>${money(online)}</strong></div><div class="stat-row"><span>Expected Offline Profit</span><strong>${money(offlineProfit)}</strong></div><div class="stat-row"><span>Expected Online Profit</span><strong>${money(onlineProfit)}</strong></div><div class="stat-row"><span>Expected Margin</span><strong>${pct(desired*100)}</strong></div></div>`; }
function calculateDiscountEngine(){ const price=num(val('discountPrice')), cost=num(val('discountCost')), commissionRate=num(val('discountCommission'))/100; const type=val('discountType'), value=num(val('discountValue')), cap=num(val('discountCap')); let discount=0; if(type==='flat') discount=value; if(type==='percent') discount=price*value/100; if(type==='percent_cap') discount=Math.min(price*value/100,cap||Infinity); const finalPrice=Math.max(0,price-discount); const commission=finalPrice*commissionRate; const profit=finalPrice-cost-commission; byId('discountOutput').innerHTML=`<div class="result-card"><h3>Discount Impact</h3><div class="stat-row"><span>Final Selling Price</span><strong>${money(finalPrice)}</strong></div><div class="stat-row"><span>Discount</span><strong>${money(discount)}</strong></div><div class="stat-row"><span>Commission</span><strong>${money(commission)}</strong></div><div class="stat-row"><span>Profit</span><strong class="${profit>=0?'profit':'loss'}">${money(profit)}</strong></div><div class="stat-row"><span>Margin</span><strong>${pct(margin(profit,finalPrice))}</strong></div></div>`; }
